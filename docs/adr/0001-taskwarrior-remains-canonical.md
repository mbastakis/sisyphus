# Keep Taskwarrior as the canonical task system

Sisyphus will remain a client over Taskwarrior and its TaskChampion replica rather than introducing a second task database. Board-specific task meaning that cannot use a native Taskwarrior field may use documented UDAs, preserving ordinary CLI access and avoiding fragile bidirectional synchronization.
